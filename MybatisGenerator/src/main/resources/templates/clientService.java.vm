package ${package.Mapper};

import ${package.Entity}.${entity};
import ${superServiceClassPackage};

/**
 * <p>
 * $!{table.comment} 服务类
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${kotlin})
interface ${table.Mapper} : ${superServiceClass}<${entity}>
#else
public interface ${table.MapperName} extends ${superServiceClass}<${entity}> {

}
#end
